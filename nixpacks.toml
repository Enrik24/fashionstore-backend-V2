[phases.setup]
nixPkgs = ["python311", "postgresql", "gcc", "libpq-dev"]

[phases.install]
cmds = [
    "pip install --upgrade pip",
    "pip install -r requirements.txt"
]

[phases.build]
cmds = ["echo 'Build phase complete'"]

[start]
cmd = "bash start.sh"
